{
  "description": "Test files that no suite runs. Every one was executed against a throwaway PostgreSQL 16 whose schema was built with drizzle-kit push, individually and together. This is a backlog with evidence, not an approval. Adding a NEW unrun test file fails server/tests/test-manifest-integrity.test.ts. Do not add entries here to silence a failure.",
  "count": 39,
  "files": [
    "server/__tests__/kevin-agent-integration.spec.ts",
    "server/tests/agent-dead-letter-reliability.test.ts",
    "server/tests/agent-outcome-attribution-migration.test.ts",
    "server/tests/agentmail-approved-send-idempotency.test.ts",
    "server/tests/agentmail-followup-migration.test.ts",
    "server/tests/agentmail-multitenant.test.ts",
    "server/tests/agentmail-p0-closure.test.ts",
    "server/tests/agentmail-p0-remediation.test.ts",
    "server/tests/agentmail-p0-surgical.test.ts",
    "server/tests/agentmail-provider-contract.test.ts",
    "server/tests/agentmail-reply-migration.test.ts",
    "server/tests/application-migrations.test.ts",
    "server/tests/attendance-migration.test.ts",
    "server/tests/autonomous-hermes-opportunity-migration.test.ts",
    "server/tests/book-funnel-migration.test.ts",
    "server/tests/cashout-tenant-isolation.test.ts",
    "server/tests/composio-connected-account-ownership-migration.test.ts",
    "server/tests/composio-specialized-version-persistence.test.ts",
    "server/tests/conflict-review-migration.test.ts",
    "server/tests/cross-agent-coordination-migration.test.ts",
    "server/tests/decision-journal-migration.test.ts",
    "server/tests/follow-up-reliability.test.ts",
    "server/tests/forecasting-migration.test.ts",
    "server/tests/graceful-shutdown-postgres.test.ts",
    "server/tests/kevin-inbox-label.test.ts",
    "server/tests/kevin-integration.test.ts",
    "server/tests/kevin-slack-tenant-isolation.test.ts",
    "server/tests/manual-payment-tenant-isolation.test.ts",
    "server/tests/payload-versioning-reliability.test.ts",
    "server/tests/required-feature-schemas.test.ts",
    "server/tests/retry-jitter-circuit-breaker.test.ts",
    "server/tests/scheduler-lock-reliability.test.ts",
    "server/tests/scheduling-migration.test.ts",
    "server/tests/schema-bootstrap.test.ts",
    "server/tests/sponsorship-partnership-migration.test.ts",
    "server/tests/unsubscribe-token-scope.test.ts",
    "server/tests/wallet-read-tenant-isolation.test.ts",
    "server/tests/workflow-idempotency-scoping.test.ts",
    "server/tests/workflow-job-reliability.test.ts"
  ],
  "diagnosis": {
    "server/__tests__/kevin-agent-integration.spec.ts": "FAILS ALONE \u2014 expects live Kevin integration configuration.",
    "server/tests/agent-dead-letter-reliability.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/agent-outcome-attribution-migration.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/agentmail-approved-send-idempotency.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/agentmail-followup-migration.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/agentmail-multitenant.test.ts": "PASSES ALONE, cannot be suited yet \u2014 it shares a database with files that DROP or TRUNCATE the tables it depends on. Blocked on the same per-file isolation.",
    "server/tests/agentmail-p0-closure.test.ts": "FAILS ALONE against a db:push-built PostgreSQL 16 \u2014 cause not yet diagnosed.",
    "server/tests/agentmail-p0-remediation.test.ts": "PASSES ALONE, cannot be suited yet \u2014 it shares a database with files that DROP or TRUNCATE the tables it depends on. Blocked on the same per-file isolation.",
    "server/tests/agentmail-p0-surgical.test.ts": "PASSES ALONE, cannot be suited yet \u2014 it shares a database with files that DROP or TRUNCATE the tables it depends on. Blocked on the same per-file isolation.",
    "server/tests/agentmail-provider-contract.test.ts": "PASSES ALONE, cannot be suited yet \u2014 it shares a database with files that DROP or TRUNCATE the tables it depends on. Blocked on the same per-file isolation.",
    "server/tests/agentmail-reply-migration.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/application-migrations.test.ts": "FAILS ALONE \u2014 the migration baseline expects a unique index (org_id,source_id) on attention_items that db:push does not create. Migrations and the Drizzle schema disagree.",
    "server/tests/attendance-migration.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/autonomous-hermes-opportunity-migration.test.ts": "FAILS ALONE \u2014 the application migration chain references a column shared/schema.ts does not define, so it cannot run against a db:push-built database. Migrations and the Drizzle schema disagree.",
    "server/tests/book-funnel-migration.test.ts": "FAILS ALONE \u2014 the application migration chain references a column shared/schema.ts does not define, so it cannot run against a db:push-built database. Migrations and the Drizzle schema disagree.",
    "server/tests/cashout-tenant-isolation.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/composio-connected-account-ownership-migration.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/composio-specialized-version-persistence.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/conflict-review-migration.test.ts": "FAILS ALONE \u2014 the application migration chain references a column shared/schema.ts does not define, so it cannot run against a db:push-built database. Migrations and the Drizzle schema disagree.",
    "server/tests/cross-agent-coordination-migration.test.ts": "FAILS ALONE \u2014 the application migration chain references a column shared/schema.ts does not define, so it cannot run against a db:push-built database. Migrations and the Drizzle schema disagree.",
    "server/tests/decision-journal-migration.test.ts": "FAILS ALONE \u2014 the application migration chain references a column shared/schema.ts does not define, so it cannot run against a db:push-built database. Migrations and the Drizzle schema disagree.",
    "server/tests/follow-up-reliability.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/forecasting-migration.test.ts": "FAILS ALONE \u2014 the application migration chain references a column shared/schema.ts does not define, so it cannot run against a db:push-built database. Migrations and the Drizzle schema disagree.",
    "server/tests/graceful-shutdown-postgres.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/kevin-inbox-label.test.ts": "FAILS ALONE against a db:push-built PostgreSQL 16 \u2014 cause not yet diagnosed.",
    "server/tests/kevin-integration.test.ts": "FAILS ALONE \u2014 expects live Kevin integration configuration.",
    "server/tests/kevin-slack-tenant-isolation.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/manual-payment-tenant-isolation.test.ts": "FAILS ALONE \u2014 written against a pre-refactor API. The storage method is now creditManualPaymentForOrganization (arguments reordered, extra 'method' parameter) and the route calls executeManualPaymentForOrganization through a service layer. The cross-organization isolation it asserts IS still enforced (membership SELECT ... FOR UPDATE in storage.ts). Stale test, not a live defect \u2014 needs a rewrite against the current architecture, not a rename.",
    "server/tests/payload-versioning-reliability.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/required-feature-schemas.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/retry-jitter-circuit-breaker.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/scheduler-lock-reliability.test.ts": "PASSES ALONE, cannot be suited yet \u2014 it shares a database with files that DROP or TRUNCATE the tables it depends on. Blocked on the same per-file isolation.",
    "server/tests/scheduling-migration.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/schema-bootstrap.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/sponsorship-partnership-migration.test.ts": "FAILS ALONE \u2014 the application migration chain references a column shared/schema.ts does not define, so it cannot run against a db:push-built database. Migrations and the Drizzle schema disagree.",
    "server/tests/unsubscribe-token-scope.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/wallet-read-tenant-isolation.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/workflow-idempotency-scoping.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first.",
    "server/tests/workflow-job-reliability.test.ts": "PASSES ALONE, cannot be suited yet \u2014 this file DROPs or TRUNCATEs shared tables in its setup, so it destroys the schema its neighbours build. Running the 26 together produced 20 failures. Wiring these up needs per-file database isolation in script/test-runner.mjs first."
  }
}
