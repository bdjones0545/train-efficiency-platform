Now perform a full audit of the forgot-password / reset-password system you just built for TrainEfficiency.

I do not want a superficial review. I want you to deeply inspect the entire flow across frontend, backend, database, auth integration, email sending, and security.

AUDIT OBJECTIVE
Find and fix any issue that could cause:
- broken reset flows
- security vulnerabilities
- role-specific failures across users/coaches/admins
- poor UX on mobile
- bad error handling
- token misuse
- inconsistent auth behavior

AUDIT EVERYTHING

1. FRONTEND FLOW
Audit:
- login page forgot-password link
- forgot-password page/modal behavior
- reset-password page behavior
- loading states
- success states
- error states
- responsiveness on mobile
- double-submit prevention
- validation messaging
- show/hide password toggle
- redirect behavior after success

Fix any broken state transitions or UI inconsistencies.

2. BACKEND RESET REQUEST FLOW
Audit:
- email normalization
- account lookup handling
- neutral response behavior to prevent user enumeration
- token generation security
- token hashing before storage
- token expiration logic
- invalidation of previous tokens
- rate limiting by IP and email
- handling of repeated requests
- logging and exception safety

Fix any weak or inconsistent logic.

3. BACKEND RESET CONFIRM FLOW
Audit:
- token verification
- hashed token comparison
- expiration enforcement
- used token prevention
- password validation enforcement
- password hashing on save
- token marked as used after success
- old token rejection after new token issued
- session invalidation if supported

Fix any issue that could allow replay, stale token use, or partial reset completion.

4. ROLE COVERAGE
Verify the flow works identically for:
- standard users
- coaches
- admins if present

Do not allow duplicated role-specific reset logic if a shared auth system can be used.
Refactor to a unified implementation where possible.

5. DATABASE / DATA LAYER
Audit:
- reset token table/schema
- indexes
- expiration fields
- usedAt handling
- cleanup strategy for old tokens
- migration correctness
- relations to auth users

Fix schema or query issues if needed.

6. EMAIL SYSTEM
Audit:
- email sending reliability
- reset link correctness
- environment variable usage
- branded template rendering
- fallback plain text version
- production-safe URL generation
- local/dev vs production reset URL behavior

Fix any hardcoded URL, broken formatting, or unsafe configuration.

7. SECURITY REVIEW
Specifically test for:
- user enumeration
- token replay
- raw token storage
- missing expiration checks
- weak password validation
- rate-limit bypass
- leaking internal errors to UI
- trusting frontend validation only
- missing auth/session revocation after reset

Patch all vulnerabilities found.

8. EDGE CASE TESTING
Simulate and verify:
- nonexistent email
- uppercase email input
- multiple reset requests
- clicking old link after newer request
- expired token
- already used token
- invalid token
- password mismatch
- network error during submit
- email send failure
- user closes flow and returns later

Fix all edge-case failures.

9. CODE QUALITY
Audit for:
- duplicate logic
- dead code
- spaghetti auth handling
- inconsistent naming
- bad error handling
- weak type safety
- missing server-side guards

Refactor where needed for maintainability.

10. FINAL OUTPUT
After auditing and fixing, provide:
- what issues were found
- what was fixed
- any remaining risks
- files updated
- migrations involved
- manual steps required
- a final verdict on whether the forgot-password feature is production-ready

IMPORTANT
Do not just describe the code. Actually inspect it, test the flows mentally/in code, fix weaknesses, and harden the implementation.
If anything is incomplete or not production-safe, fix it now.